<view class="wx-popup" hidden="{{flag}}">
  <view class='popup-container'>
    <view class="wx-popup-title">{{title}}</view>
    <view class="wx-popup-con">
      <!-- <text wx:for='{{content}}' wx:key="idx" wx:for-item="item">{{item.matter_name}}</text> -->
      <checkbox-group bindchange="checkGroupChange">
        <label wx:for="{{content}}" wx:key="idx" wx:for-item="item">
          <checkbox value="{{item.matter_name}}" checked="{{item.checked}}"></checkbox>
          <text>{{item.matter_name}}</text>
        </label>
      </checkbox-group>
    </view>
    <view class="wx-popup-btn">
      <text class="btn-no" bindtap='_error'>{{btn_no}}</text>
      <text class="btn-ok" bindtap='_success'>{{btn_ok}}</text>
    </view>
  </view>
</view>
